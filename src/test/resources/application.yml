spring:
    datasource:
        password: sa
        url: jdbc:h2:mem:gestor_docentes_db
        username: sa
        driverClassName: org.h2.Driver
    jpa:
        spring:
            jpa:
                database-platform: org.hibernate.dialect.H2Dialect
        database: h2